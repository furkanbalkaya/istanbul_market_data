{
    "builds": [
      {
        "src": "path/to/your/app.py",
        "use": "@vercel/python",
        "config": {
          "distDir": "output"
        }
      }
    ],
    "buildCommand": "pip install setuptools==72.1.0 distutils && vercel build"
  }
  